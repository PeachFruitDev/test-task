package test.interview.reporter;

public class OrgStructReporter {

    // No static keyword as this class won't be used in static context (rather a bad practice)
    private final String PROPERTY_NAME = "report.properties";

    public String report() {
        return "";
    }
}
